git config --global user.name "John Doe"
git config --system user.email "jdoe@example.com"
git config --list
git config user.name

git help <command>
git <command> --help
man git-<command>

git init
git clone <repo_link>

git status
git status -s, git status --short

git add <files>

git tag
git tag -l <pattern>
git tag -a <tagName> -m <comment>
git tag -s <tagName> -m <comment>
git tag <lightweightTagName>
git tag -a <tagName> <commitHash> -m <comment>
git push origin <tagName>
git tag -d <tagName>
git push origin :<tagName>
git push origin --tags

git log - ≥стор≥€ у формат≥ за замовчуванн€м
git log -p|--patch - разом ≥з зм≥нами
git log <n> - обмеженн€ к≥лькост≥ елемент≥в
git log --stat - статистика зм≥н
git log --pretty=full|fuller|oneline|format:Ф<format>Ф - зм≥на формату виводу
git log --oneline - статистика зм≥н в короткому формат≥
git log --graph - ≥стор≥€ у вигл€д≥ ASCII-графу
git log --grep - пошук по commit message
git log --since/--until - пошук по дат≥
git log -S - пошук по вм≥сту зм≥н
git log -- <path/to/file> - ком≥ти €к≥ модиф≥кували файл